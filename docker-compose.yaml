version: "3.2"
services:

  redis:
    image: "redis:alpine"
    ports:
     - "6379:6379"

    volumes:
      - $PWD/data/redis:/var/lib/redis
  
  server:
    depends_on:
      - redis
    build:
      context: .
    environment:
      REDIS_HOST: redis
      NOTIFY_LOG: matches.json
    volumes:
      - $PWD:/go/rezen/monitor-certs
  
